const expect = require('expect');
const request = require('supertest');
const { app } = require('./../server')
var { Todos } = require('./../models/Todos');
var { Users } = require('./../models/Users');

describe('POST /todos', () => {
    it('it should create a todo data', (done) => {

    })
})